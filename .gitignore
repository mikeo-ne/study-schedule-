# Dependencies
node_modules/

# Local env / secrets — NEVER commit private keys or API creds
.env
.env.local
*.key

# Runtime state written by the agent
data/state.json
data/*.log
logs/

# OS junk
.DS_Store
